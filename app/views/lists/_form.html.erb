<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='field'>
    <%= f.label :title%>
    <%= f.text_field :title%>
  </div>

  <%= f.fields_for :steps do |steps_form|%>

    <div class="field">
      <%= steps_form.label :item %><br>
      <%= steps_form.text_field :item %>
    </div>
    <div class="field">
      <%= steps_form.label :title %><br>
      <%= steps_form.text_field :title %>
    </div>
    <div class="field">
      <%= steps_form.label :explination %><br>
      <%= steps_form.text_area :explination %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
